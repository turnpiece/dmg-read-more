# Database credentials for WordPress test environment

DB_NAME=local
DB_USER=root
DB_PASS=root
DB_HOST=localhost

# Use this format if you're running LocalWP and need to connect via socket:
# DB_HOST=localhost:/Users/yourname/Library/Application Support/Local/run/<id>/mysql/mysqld.sock
